create database selfjoindb;

use selfjoindb;

create table employees(eid int,name varchar(20),manager_id int);

insert into employees values
(1,'tushar',null),(2,'abhinav',1),(3,'saksham',1),(4,'akshay',1);

SET SQL_SAFE_UPDATES = 0;

UPDATE Employees
SET Manager_ID = 2
WHERE eid = 4;

SET SQL_SAFE_UPDATES = 1;


select * from employees;

select emp.eid,emp.name,emp.manager_id,mngr.eid,mngr.name from employees as emp
join employees as mngr on emp.manager_id=mngr.eid;

select emp.eid,emp.name,emp.manager_id,mngr.eid,mngr.name from employees as emp
left join employees as mngr on emp.manager_id=mngr.eid;

